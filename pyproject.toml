[project]
name = "fe-cr"
version = "0.1.0"
description = "Generador de comprobantes electrónicos de Costa Rica versión 4.4"
readme = "README.md"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.10"
dependencies = []

[project.urls]
homepage = "https://example.com/fe-cr"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
